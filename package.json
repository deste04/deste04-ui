{
  "name": "deste04-ui-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo della libreria deste04-ui: CLI per installare i componenti + sito che li mostra",
  "workspaces": [
    "web",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace web",
    "cli": "node packages/cli/src/cli.js"
  }
}
